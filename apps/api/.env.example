# Environment (set to "production" in Railway, omit or set to "development" locally)
NODE_ENV=production

# Database
DB_USERNAME="peppermint"
DB_PASSWORD="1234"
DB_HOST=localhost
DATABASE_URL="postgresql://${DB_USERNAME}:${DB_PASSWORD}@${DB_HOST}/peppermint"

# JWT Secret
SECRET="supersecret"

# Frontend URL (for CORS - required for production)
FRONTEND_URL=https://your-frontend.railway.app

# OAuth (optional)
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REDIRECT_URI=
